{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [
                      "--research_config=r_train_qmix_8m_debug",
                      "--batch_job=False",
                      "--use_cuda=True"
                    ]
        }
    ]
}